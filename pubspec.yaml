name: testing_chart_crypto
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  fl_chart: ^0.63.0
  flutter:
    sdk: flutter
  syncfusion_flutter_charts: ^22.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
